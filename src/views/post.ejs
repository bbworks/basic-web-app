<html>
  <head>
    <meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <meta name="description" content="This is a post.">
    <link type="image/ico" rel="icon" href="https://cdn.icon-icons.com/icons2/221/PNG/64/Archivos_25593.png">
    <title>App</title>

    <!-- FONT AWESOME -->
    <!-- NOTE: Import Font Awesome first, so we can override the formating of their icons -->
    <script src="https://kit.fontawesome.com/4849833081.js" crossorigin="anonymous"></script>

    <!-- STYLESHEETS -->
    <link type="text/css" rel="stylesheet" href="/css/post.css">
  </head>
  <body>
    <%- include ("partials/header") %>
    <main id="content" class="content">
      <section id="post-<%= post.post_id %>" class="post-section">
      	<div class="post-header">
          <span class="post-heading"><%= post.heading %></span
        	><div class="post-author-container">
        		<img class="post-author-photo" src="<%= post.user_photo_url %>" /
        		><span class="post-author-name"><%= post.first_name+' '+post.last_name %></span>
        	</div
        	><span class="post-date"><%= post.post_date %></span>
        </div>
        <img class="post-image" src="<%= post.post_photo_url %>" /
      	><p class="post-body"><%= post.body %></p>
      </section>
    </main>
    <%- include ("partials/footer") %>
  </body>
  <script src="/js/currentPage.js"></script>
</html>
